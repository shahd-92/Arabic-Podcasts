<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udacity.shahd.arabicpodcasts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NowPlaying"
            android:label="@string/NowPlaying" />
        <activity android:name=".AddPodcast"
            android:label="@string/AddPodcast" />
        <activity android:name=".DownloadedPodcasts"
            android:label="@string/DownloadedPodcasts" />
        <activity android:name=".PodcastInfo"
            android:label="@string/PodcastInfo"></activity>
    </application>

</manifest>